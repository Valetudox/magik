services:
  schemathesis:
    image: python:3.12-slim
    container_name: backend-tabledocument-schemathesis-deployed
    volumes:
      - ../../../specs/domains/tabledocument/openapi.yaml:/schema/openapi.yaml:ro
      - ./schemathesis-tests:/tests:ro
    working_dir: /tests
    command: >
      sh -c "pip install -q -r requirements.txt &&
             pytest test_tabledocument_api.py --base-url=http://host.docker.internal:3004 -v --tb=line -rA --hypothesis-show-statistics"
    network_mode: host
