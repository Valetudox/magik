// Type definitions for backend-tabledocument service
// Note: Some types are currently defined in service files and could be moved here for better organization

export interface TableDocumentResponse {
  id: string
  [key: string]: unknown
}
